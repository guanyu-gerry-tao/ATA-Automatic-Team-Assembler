# AI-GENERATED CODE

This documentation and related build scripts were automatically generated by an AI assistant.

---

# Simple Usage Guide

## 📝 Local Development

1. Copy `.env_fe` to `.env` in `FE_Student/` directory:
   ```bash
   cp .env_fe .env
   ```

2. Edit `.env` file and set your backend URL:
   ```
   VITE_API_BASE_URL=http://localhost:8000
   ```

3. Run the build script:
   ```bash
   node build.js
   ```

4. Open `index.html` in your browser!

## 🚀 Vercel Deployment

1. In Vercel project settings → Environment Variables, add:
   - Variable name: `VITE_API_BASE_URL`
   - Value: Your backend server URL (e.g., `https://api.example.com`)

2. Commit and push your code - Vercel will automatically build ✅

That's it!